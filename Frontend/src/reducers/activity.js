let initialState = {
    runs: [],
    walks: [],
    swims: [],
    bikes: []
}

export default (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state
        }
    }
}