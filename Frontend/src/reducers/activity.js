let initialState = {
    activities: ["run ","walk ","swim"]
}

export default (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}